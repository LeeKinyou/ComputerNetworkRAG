# 07 · 开发计划与任务拆解

> 版本 v0.1 ｜ 2026-09-08 ｜ 状态：待评审
> 本计划按 writing-plans 规范编写：每个任务给出文件、对外接口与独立验收方式；执行时逐项勾选。
> 上游基线：01–06 号文档。**Goal：交付可现场演示的单进程课程智能问答 MVP。**

## Global Constraints（全局约束，每个任务默认遵守）

- Python 3.12 锁定，统一用 uv 管理环境与依赖（`uv sync` 安装、`uv run` 执行、`uv add` 加依赖）；依赖以 02 号文档 §5 选型表为准，不自行新增重依赖；
- 单进程、同源静态托管；不引入任何外部数据库/中间件/容器（LangGraph 检查点同样是本地 SQLite 文件）；
- 密钥只从 `app/config.py` 读取；新增配置项必须同步更新 `.env.example` 与 02 号文档 §7；
- 智能体编排固定 LangChain/LangGraph 1.x：统一 `langchain.agents.create_agent`，不写 0.x AgentExecutor、不手工拼 StateGraph；框架只允许出现在 `builder.py`/`runner.py`，业务工具禁止 import LangChain；
- 模块/端点/事件/工具/表名必须与 03/04/05 号文档一致；变更先改文档；
- 纯算法工具（subnet/lpm/number）必须配 pytest 用例；提交前本地跑通；
- 每个任务结束时更新本文件勾选状态并提交一次 git commit。

## 里程碑总览

| 里程碑 | 目标 | 出口标准 |
|---|---|---|
| M0 工程骨架 | 单进程跑起空框架 | 访问 8000 看到五视图骨架；/api/health 返回正常 |
| M1 入库与图谱 | 课程材料可入库、图谱可看 | md/docx 上传建库，图谱页渲染实体关系 |
| M2 RAG 问答 | 流式问答闭环 | hybrid 问答流式输出 + 来源卡片 |
| M3 ReAct 智能体 | 工具调用全流程可视化 | 四工具 + 时间线 + trace 回放 |
| M4 打磨与交付 | 演示稳定美观 | 01 号文档 §6 验收清单全部通过 |

---

## M0 · 工程骨架

### Task 0.1 初始化工程与配置

**Files**
- Create: `pyproject.toml`（`uv init`，requires-python = ">=3.12,<3.13"）、`.python-version`（3.12）、`uv.lock`、`.env.example`、`.gitignore`、`main.py`、`app/__init__.py`、`app/config.py`、`app/schemas.py`

**Interfaces（本任务产出，后续依赖）**
- `app/config.py`：`class Settings(BaseSettings)` 字段与 02 §7 一致；`get_settings() -> Settings`（lru_cache 单例）。

- [x] `uv init` 后用 `uv add` 写入依赖：fastapi、uvicorn[standard]、sse-starlette、langchain、langgraph、langchain-openai、langgraph-checkpoint-sqlite（均锁定 1.x 主版本；openai/httpx 由 langchain-openai 传递引入）、pydantic、pydantic-settings、lightrag-hku、python-docx、dnspython；`uv add --dev pytest pytest-asyncio`（pypdf/python-pptx/icmplib 注释为 P1，暂不 add）；完成后 `uv sync`
- [x] `.env.example` 按 02 §7 全量列出；`.gitignore` 忽略 `.env`、`data/`、`.venv/`、`__pycache__/`
- [x] `main.py`：创建 FastAPI、CORS 仅同源、`if __name__ == "__main__": uvicorn.run("main:app", host=settings.APP_HOST, ...)`
- [x] 验证：`uv run python main.py` 启动无报错；`GET /api/health` 返回 `{"status":"ok"}`

### Task 0.2 静态骨架与五视图

**Files**
- Create: `static/index.html`、`static/assets/css/app.css`、`static/assets/js/app.js`、`static/assets/js/api.js`、`static/assets/js/views/*.js`（五个空视图）
- Modify: `main.py` 挂载 `StaticFiles(directory="static", html=True)` 于根路径（API 路由先注册）

**Interfaces**
- `api.js`：`getJSON(path)`、`postJSON(path, body)`；`postSSE(path, body, handlers) -> AbortController`（按 06 §4.4.2 实现半包解析）。

- [x] 实现顶部标题栏 + 五 Tab + 状态点；CSS 写入 06 §2 全部设计令牌
- [x] CDN 引入 vue@3.4 / echarts@5.5，`lib/` 本地兜底切换逻辑
- [x] 验证：五个 Tab 切换正常、刷新保持锚点；断网拔 CDN 时显示本地副本而非白屏

### Task 0.3 SQLite 仓储

**Files**
- Create: `app/storage/repository.py`、`app/storage/sqlite_repo.py`
- Modify: `main.py` lifespan 中初始化建表（DDL 见 03 §6.1）

**Interfaces（其他模块只依赖抽象）**
- `Repository` 抽象方法签名与 03 §6.2 一致；`SqliteRepository(db_path: Path)`；`get_repo() -> Repository`。

- [x] 实现 documents/sessions/messages/agent_runs/agent_steps 五张表的方法
- [x] 编写最小冒烟脚本：插入会话→消息→运行→两步→读回断言一致
- [x] 验证：重复启动不报错（IF NOT EXISTS）；data/app.db 生成

---

## M1 · 文档入库与知识图谱

### Task 1.1 Parser 注册表与文本解析

**Files**
- Create: `app/parsers/base.py`、`app/parsers/text_parser.py`、`app/parsers/docx_parser.py`、`app/parsers/pdf_parser.py`、`app/parsers/pptx_parser.py`、`app/parsers/__init__.py`

**Interfaces**
- `BaseParser.parse(file_path) -> ParsedDocument`；`register()` 装饰器；`get_parser(filename) -> BaseParser`；异常 `UnsupportedFormatError`、`NotEnabledError`。

- [x] TextParser 支持 .md/.txt（utf-8→gbk 回退）；DocxParser 按段落+表格顺序输出 markdown 标题层级
- [x] pdf/pptx 类注册但抛 NotEnabledError
- [x] 测试：tests/test_parsers.py，md 与 docx 各 1 个样例，断言输出含指定标题文本

### Task 1.2 LightRAG 工厂

**Files**
- Create: `app/rag/lightrag_factory.py`、`app/rag/__init__.py`

**Interfaces**
- `get_lightrag()` 单例；`llm_model_func`（httpx 异步走 LLM_BASE_URL，超时 LLM_TIMEOUT）；`embedding_func`（批量）；方法 `ainsert_text(text)`、`aquery(question, mode)`、`get_graph_snapshot(limit, keyword, entity_type)`、`get_entity_subgraph(name, k=1)`。

- [x] 用 .env 中真实 key 跑通：插入两段网络课程文本 → hybrid 查询返回合理答案
- [x] 领域抽取 prompt 限定实体类型（协议/设备/层次/地址/算法/性能指标/概念）
- [x] 验证：data/lightrag 生成四类存储文件；重启进程后已建知识仍可查询

### Task 1.3 入库流水线与文档 API/页面

**Files**
- Create: `app/services/ingestion_service.py`、`app/api/documents.py`
- Modify: `main.py` 注册路由；`static/.../views/library.js`、`overview.js`

**Interfaces**
- `IngestionService.ingest(doc_id)`（后台任务）、端点按 04 §3 实现；`GET /api/stats`。

- [x] 上传→落盘→pending→解析缓存 parsed/→ainsert→indexed，状态全程可查
- [x] 管理页上传/状态表/重建/删除；概览卡片接 /api/stats
- [x] 验证：上传 ≥3 份材料（含 1 docx）全部 indexed；上传 .pdf 返回 unsupported 友好提示；断 key 时该篇 failed 且其他篇不受影响（tests/test_ingestion_service.py 故障注入；真实事故佐证：docx 抽取超时 failed 时另两篇保持 indexed）

### Task 1.4 图谱 API 与图谱页

**Files**
- Create: `app/services/graph_service.py`、`app/api/graph.py`、`static/.../components/graph-view.js`、`views/graph.js`

**Interfaces**
- 端点按 04 §4；ECharts graph 配置按 06 §4.2。

- [x] nodes/edges 导出 + 类型聚合 + 两端过滤 + limit 截断标记
- [x] 力导向图：类型配色/搜索/筛选/点击抽屉（详情+邻居+来源）
- [x] 验证：能搜到"TCP"并展示详情；>500 节点出现截断提示（浏览器实测搜索 TCP→抽屉详情/邻居/来源齐全；截断横幅依赖 `truncated` 字段，已用 limit=1 参数级验证，当前库 52 节点暂无 >500 真实数据）

---

## M2 · RAG 问答

### Task 2.1 RAG 服务与 SSE 端点

**Files**
- Create: `app/services/rag_service.py`、`app/api/rag.py`
- Modify: `main.py` 注册路由

**Interfaces**
- `RAGService.stream_answer(question, mode, session_id) -> AsyncIterator[Event]`，事件 meta/sources/token/final/error（04 §5）。

- [x] 模式枚举校验，非法值回退默认并在 meta 标注；sources 整理为 doc_name/chunk_id/snippet
- [x] 最终生成走流式接口逐 token 产出；sessions/messages 落库（llm_model_func 支持 stream=True 返回 token 迭代器；astream_query 单次 aquery_llm 同取来源分块与 token 流，省一次关键词提取；问题过短按 LightRAG 权重阈值 422）
- [x] 测试：tests/test_rag_service.py mock LLM，断言事件顺序与 sources 结构（curl 实测：meta→sources→234 token→final，会话/双消息落库，非法模式 meta 标注 mode_requested）

### Task 2.2 RAG 问答页

**Files**
- Create: `static/.../views/rag.js`、`components/source-card.js`；`app/api/sessions.py`（会话列表/详情）

- [ ] 模式切换、对话气泡、token 逐字、来源折叠卡片、错误重试、历史会话
- [ ] 验证：naive 与 hybrid 对同一问题结果存在可见差异；刷新后历史还在

---

## M3 · ReAct 智能体

### Task 3.1 工具基类与两个纯算法工具（TDD）

**Files**
- Create: `app/agent/__init__.py`、`app/agent/registry.py`、`app/agent/tools/__init__.py`、`tools/subnet.py`、`tools/lpm.py`、`tests/test_subnet.py`、`tests/test_lpm.py`

**Interfaces**
- `BaseTool`（含 `requires_approval` 字段）/`ToolResult`/`ToolRegistry`（含 `to_langchain_tools()` 适配器，03 §4.2、05 §1.3）；工具名固定 `subnet_calculator`、`lpm_lookup`。

- [ ] 先写测试：subnet 覆盖 /24、/27、/16、/32、非法输入，与 `ipaddress` 标准库对拍；lpm 覆盖最长命中/仅默认路由/无路由
- [ ] 实现至全部通过；subnet structured 字段与 05 §2.1 完全一致；lpm 输出 trace
- [ ] 验证：`uv run pytest -q` 全绿

### Task 3.2 DNS 与课程检索工具

**Files**
- Create: `tools/dns.py`、`tools/course_rag.py`
- Modify: `tools/__init__.py` 注册全部四个工具

**Interfaces**
- `course_rag_query` 构造注入 RAGService；输出限长 1500 字 + sources（05 §2.4）。

- [ ] dns 3s 超时、rtype 枚举、非法域名 failed；http 工具的安全校验函数同步写好备用
- [ ] 测试：dns 用可重放的 mock resolver；course_rag 用 mock RAGService
- [ ] 验证：真实查询 www.example.com 返回 A 记录

### Task 3.3 create_agent 构建器与流式事件映射

**Files**
- Create: `app/agent/builder.py`、`app/agent/runner.py`

**Interfaces**
- `build_agent(registry, rag_service) -> CompiledStateGraph`（create_agent + ChatOpenAI(base_url=) + AsyncSqliteSaver，03 §3.2）；
- `run_agent(agent, question, run_id, enabled_tools=None) -> AsyncIterator[Event]`，事件集与 04 §6.2 一致；system prompt 采用 05 §5 草案。

- [ ] builder：模型/工具适配器/system prompt/checkpointer 组装；`recursion_limit=AGENT_RECURSION_LIMIT`
- [ ] runner：`astream(stream_mode=["updates","messages"])` 映射——model 节点 AIMessage→thought/action、tools 节点 ToolMessage→observation、model 消息块→token（注意 1.x 节点名为 model）
- [ ] 工具异常在适配器内转 failed ToolMessage 回灌，不炸图；递归触顶按框架终止并在 final 标注 truncated
- [ ] 测试：用 LangGraph FakeToolCallingModel（或脚本化假模型）产出"一次工具调用→最终回答"，断言映射后事件序列 step_start/thought/action/observation/token/final
- [ ] 验证：复合问题能连续调用两个工具；data/langgraph.db 正常生成

### Task 3.4 Agent 服务、SSE 端点与 trace 落库

**Files**
- Create: `app/services/agent_service.py`、`app/api/agent.py`；Modify: `app/api/sessions.py`（runs 回放端点）

- [ ] run 生命周期：create_run→逐事件 SSE + append_step→finish_run；assistant 消息落 messages
- [ ] GET runs 回放接口（04 §6.4）
- [ ] 验证：SSE 事件与库中 agent_steps 一一对应；中途 kill 客户端后 run 标记 failed 而非悬挂

### Task 3.5 智能体页与时间线组件

**Files**
- Create: `static/.../components/timeline.js`、`views/agent.js`

- [ ] 三类卡片按 06 §4.4 渲染；subnet 结构化键值表、lpm trace 表；耗时/成败/重试；预置问题 chips（05 §6）
- [ ] 历史回放复用 timeline.js
- [ ] 验证：完整跑通 01 §6 第 5 条验收样例（/27 计算 + 网关概念）

### Task 3.6 人工审批节点（P1，架构已在 3.3 预留）

**Files**
- Create: `app/api/approval.py`；Modify: `app/agent/builder.py`（审批策略）、`app/services/agent_service.py`（resume 流）、`static/.../components/timeline.js`（审批卡片）

**Interfaces**
- `POST /api/agent/runs/{run_id}/resume`（04 §6.5）；SSE 事件 interrupt/resumed（04 §6.2）。

- [ ] 二选一定稿实现：`interrupt_before=["tools"]` + `Command(resume=...)`，或 `HumanInTheLoopMiddleware`；仅 `HITL_ENABLED=true` 且工具在 `APPROVAL_TOOLS` 时生效
- [ ] 三种决策：approve 原参执行 / edit 用 edited_args 替换 / reject 回注拒绝结果让模型重决策；同一 thread_id=run_id 恢复
- [ ] 前端审批卡片（06 §4.4.1）：参数可编辑、三按钮、续跑事件追加到原时间线；刷新页面后"待审批"运行仍可继续
- [ ] 测试：脚本化图在 tools 前中断→断言 interrupt 事件与持久化状态→resume(approve)→断言 observation/final；reject 路径不执行工具
- [ ] 验证：开启审批后 ping_host 必须点批准才发出真实请求

---

## M4 · 打磨与交付

### Task 4.1 概览与整体视觉走查

- [ ] 五视图视觉走查 06 号文档全部条款（字号/间距/空态/错误态/1366×768）
- [ ] 预置问题、中文名映射、结构化结果表格突出
- [ ] 出口：找一位未参与开发的人按页面操作无卡点

### Task 4.2 稳定性与异常演练

- [ ] 错 key、断网、超时、上传坏文件、非法 CIDR、不存在域名 5 类异常逐一演练，界面均有明确提示
- [ ] 连续 20 轮问答无进程崩溃、内存无显著增长
- [ ] `/api/health?check_llm=1` 接入状态点

### Task 4.3 部署交付物

**Files**
- Create: `run.bat`、`docs/08-部署手册与演示脚本.md`（已先行撰写，按实际校正）、`README.md`（项目根，最小启动说明）

- [ ] 全新目录模拟迁移：拷贝项目（含 data/、不含 .venv）→ `uv sync` → 一条命令启动
- [ ] 任务管理器确认仅一个 python 进程（验收 01 §6 第 8 条）

### Task 4.4 验收回归

- [ ] 逐条执行 01 §6 的 8 条验收标准并记录结果
- [ ] `uv run pytest` 全绿；FastAPI `/docs` 逐接口手工过一遍
- [ ] 输出演示录屏备份（5 分钟，按 08 号文档演示脚本）

---

## 任务依赖图

```
M0.1 → M0.2 → M0.3
M0.3 → M1.1 → M1.2 → M1.3 → M1.4
M1.2 → M2.1 → M2.2
M0.3 → M3.1 → M3.2 → M3.3 → M3.4 → M3.5 → M3.6(P1)
M1.3/M1.4/M2.2/M3.5/M3.6 → M4.1 → M4.2 → M4.3 → M4.4
（M2 与 M3 在 M1 完成后可并行）
```

## 估算与排期建议

| 里程碑 | 预估工作量（单人） |
|---|---|
| M0 | 0.5 天 |
| M1 | 1.5–2 天（主要时间在 LightRAG 接入调通与抽取效果观察） |
| M2 | 0.5–1 天 |
| M3 | 2–2.5 天（create_agent 事件映射时序、前端时间线是精细活；P1 审批另计 0.5–1 天） |
| M4 | 0.5–1 天 |
| 合计 | 约 6–8 个工作日（P0 主链路约 5–6 天，P1 审批约 0.5–1 天） |

## 自检记录（写完计划后的覆盖核对）

- 01 号文档 P0 需求映射：FR-ING-01/02/05/06/07/08→M1；FR-GRAPH-*→M1.4；FR-RAG-*→M2；FR-AGT-*→M3；FR-TOOL-01~04/08→M3.1/3.2；FR-SYS-*→M0/M4；NFR 各项→M4 演练。
- P1 需求均已在对应任务留接口位（parser 占位、工具占位、布局切换、停止生成）；FR-AGT-08 人工审批对应 Task 3.6，其前提（checkpointer、thread_id=run_id、interrupt 事件位）已在 Task 3.3 落地，不返工。
- 命名一致性：端点、事件名、工具名、表名与 03/04/05 文档逐项对齐。
